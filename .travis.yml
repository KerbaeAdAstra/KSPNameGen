dist: trusty
sudo: required
language: csharp
solution: KSPNameGen.sln
jobs:
  include:
    - stage: build
      script: make all clean
    - stage: deploy
      script: sudo make install clean
    - stage: verify
      script: kspng -v
      script: kspng -h
    - stage: test
      script: kspng -b $RANDOM -n $RANDOM
      script: kspng -n $RANDOM -b $RANDOM
notifications:
  irc:
    channels:
      - "chat.freenode.net#kerbaeadastra"
    on_success: always
    on_failure: always
    use_notice: true
    skip_join: true
    template:
      - "%{repository_slug}#%{build_number} (%{commit} on %{branch} by %{author}): %{message}"
      - "Changeset: %{compare_url}"
      - "Build details: %{build_url}"
      
